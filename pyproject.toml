[project]
name = "self-service-game"
version = "0.1.0"
description = ""
authors = [
    {name = "pali",email = "prclash13@protonmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]

[tool.poetry]
packages = [{include = "self_service_game", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
